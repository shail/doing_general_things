<%= form_for @sandwich do |f| %>
	<%= f.label :sandwich_type %>
	<%= f.select :sandwich_type, [['Meaty'], ['Vegetarian'], ['Vegan']] %>

	<%= f.hidden_field :user_id, :value => current_user.id %>

	<%= f.submit "GIVE ME SANDWICH", class: "btn btn-success" %>
<% end %>